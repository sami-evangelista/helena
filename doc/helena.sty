\usepackage{listings}

\definecolor{helenakeyword}{rgb}{0,0,1}
\definecolor{helenacomment}{rgb}{0.6,0,0}

\lstdefinelanguage{helena} {
  morekeywords =
  {
    accept,
    and,
    assert,
    capacity,
    card,
    case,
    constant,
    deadlock,
    default,
    description,
    dom,
    else,
    empty,
    enum,
    epsilon,
    exists,
    false,
    for,
    forall,
    function,
    guard,
    if,
    import,
    in,
    init,
    inhibit,
    let,
    list,
    ltl,
    max,
    min,
    mod,
    mult,
    not,
    of,
    or,
    out,
    pick,
    place,
    pred,
    priority,
    property,
    proposition,
    product,
    range,
    reject,
    return,
    safe,
    set,
    state,
    struct,
    subtype,
    succ,
    sum,
    transition,
    true,
    type,
    until,
    vector,
    while,
    with
  },
  morecomment=[l]{//},
  morecomment=[n]{/*}{*/},
  keywordstyle=\color{helenakeyword}\bfseries,
  commentstyle=\color{helenacomment}\it,
  stringstyle=\ttfamily,
  morestring=[b]",
}
